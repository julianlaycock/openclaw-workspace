# 2026-03-01 â€” Landing Page v2 Deployed + CLAUDE.md Created

## Landing Page v2 â€” Shipped to Production
- **Commit:** `ceb3f8be` pushed to main, Railway auto-deploy
- **14â†’9 sections:** Merged platform+copilot, API+open-source, CTA+FAQ; removed process section; moved readiness into features header; flipped pricing to light theme
- **New features:** Mobile nav toggle (fullscreen overlay), lang switcher (ENâ†”DE), cookie consent, gradient border on featured pricing, dashboard glow animation, terminal scanline, button glow follow, FAQ left border highlight
- **DE translation:** Full German landing page created via `deploy-landing-v2.mjs` script
- **Files:** `src/frontend/public/static/landing-{en,de}.html` (55KB EN, 56KB DE)
- **QA:** 63/67 checks passed (4 skipped = responsive viewport + DE parity visual confirm)
- **Zero failures** in browser automation testing

## QA Infrastructure Created
- `LANDING-QA-CHECKLIST.md` â€” 67 checks across 14 categories (markdown)
- `landing-qa-checklist.html` â€” Interactive HTML checklist with click-to-cycle checkboxes, localStorage persistence, score bar, export, visual fingerprint section
- Both in workspace root

## CLAUDE.md â€” Project Intelligence File
- Created at `C:\Users\julia\openclaw-workspace\CLAUDE.md`
- Needs to be copied to project repo (`C:\Users\julia\projects\private-asset-registry_Caelith_v2\CLAUDE.md`)
- Contains: architecture map, design system, Railway rules, 8 common mistakes, PowerShell gotchas, git workflow, business context, pricing, key URLs
- Purpose: every new session/sub-agent reads this first to avoid repeating past mistakes

## Finch Capital Prep â€” Open-Core Defense Added
- Added full "Open-Core Defense" section to `research/finch-capital-prep-2026-03-02.html`
- 3-layer argument: what's open vs paid, why VCs should love it, competitive moat
- 2 pushback handlers: Big 4/Amazon clone, close-source question
- Key line: "The open library is like giving away a hammer. We sell the construction company."

## Production URL Verification
- `/api/blog/aifmd-annex-iv-xml-guide` â†’ 200 âœ…
- `/api/blog/eu-sanctions-screening-guide` â†’ 200 âœ…
- `/readiness-check` â†’ 200 âœ…
- `/demo-dashboard` â†’ redirects to landing (known issue, not fixed)

## Key Scripts Created
- `deploy-landing-v2.mjs` â€” Generates EN+DE from preview, adds mobile nav + lang switcher, copies to project static dir
- `landing-v2-preview.html` â€” Standalone preview of the v2 design (source of truth before deploy)

## TODO
- [ ] Copy CLAUDE.md to project repo and commit
- [ ] Verify Railway deploy landed (check https://www.caelith.tech/api/landing)
- [ ] Test responsive (768px, 480px) on production
- [ ] Fix `/demo-dashboard` redirect loop on production

---

## Evening Session — Readiness Check Bugfixes + Landing Audit (18:00–18:42 CET)

### Commits pushed (in order)
- e937c85 — fix: remove broken font-family quotes in readiness check CTA (JS syntax crash)
- 8eacd04d — fix: ansNa?N/A translation key, fix quiz nav overlap (padding-top)
- 0cbf83b — fix: quiz screen padding-top 76px for nav overlap, German unicode escapes in CTA

### Root Causes Found & Fixed

**ansNa bug:**
- Button key generated as 'ans' + v.charAt(0).toUpperCase() + v.slice(1) ? for 'na' = 'ansNa'
- Translation had nsNA (capital NA) — case mismatch ? t('ansNa') returned literal key 'ansNa'
- Fix: renamed both EN+DE translation keys from nsNA to nsNa to match generated key

**JS syntax crash (readiness check CTA blank page):**
- New CTA block had ont-family:'Sora' inside single-quoted JS strings
- 'ans' + ...'<h3 style="font-family:'Sora',...' — inner quotes terminated the outer string
- Fix: removed quotes from Sora font-family (single-word fonts don't need quotes in CSS)
- NOTE: ont-family:'Sora' in CSS stylesheets (not JS strings) is VALID — don't confuse the two

**Nav overlap (fixed pill covering quiz content):**
- Fixed pill nav at top:16px, ~44px tall — occupies ~60px from top
- Quiz screen had no top padding ? content started at top of viewport behind the pill
- Fix: added style="padding-top:76px" to #screen-quiz div
- progress-wrap sticky was also set to 	op:72px

**German text mojibake in new CTA:**
- Raw UTF-8 chars (ä, ß, ü) in the CTA JS strings were corrupting after write operations
- Fix: replaced with \\u00e4, \\u00df, \\u00fc unicode escapes in JS strings
- These render correctly in browser but survive file encoding operations safely

### Landing Page Full Audit Result: 43/43 ?
- All routes: / /api/landing?lang=de /readiness-check /demo-dashboard /login /api/copilot-demo /api/docs /blog /compare/anevis /privacy /impressum ? all 200
- All nav links, section anchors, CTAs, footer links, special chars (€ ? ? ü ö), countdown, cookie consent ? all present
- DE landing: clean (no mojibake, umlauts ü ö present)
- Mobile overflow CSS patches confirmed present

### Key Pattern Learned
- Complex HTML patches: always use Node.js .mjs scripts, never PowerShell string operations
- For German/special chars in JS strings inside template literals ? use unicode escapes, not raw UTF-8
- When auditing 'font-family:Sora in JS strings' vs CSS — CSS is fine, only JS strings matter
- The audit check for font-family was a false negative (CSS stylesheet instances are valid)
